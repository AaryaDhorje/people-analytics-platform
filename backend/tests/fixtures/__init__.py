"""Hand-computed test fixtures.

tiny_org.py (phase 3) builds a 12-employee, 18-month organization small enough
that every metric in docs/METRICS.md can be computed by hand. Expected values in
the metric tests show their arithmetic in a comment so a reviewer can check the
math without trusting the implementation.
"""
