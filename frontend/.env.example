# Copy to .env.local for local dev. Vite only exposes VITE_-prefixed variables.

# Local backend. In Vercel, set this to the Render service URL.
VITE_API_URL=http://localhost:8000

# Must match DEMO_BEARER_TOKEN in the backend .env.
VITE_DEMO_TOKEN=dev-demo-token-change-me
